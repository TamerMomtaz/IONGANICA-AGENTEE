# ═══════════════════════════════════════════════════════════════
# 🌊 A-GENTEE v5.1 — THE WAVE — Dependencies
# Updated: February 5, 2026
# Python: 3.12+ recommended (3.14 beta works but has quirks)
# ═══════════════════════════════════════════════════════════════

# ── THE MIND (4-engine ensemble) ──
anthropic>=0.40.0          # Claude API — deep reasoning, Arabic, creative
openai>=1.0.0              # OpenAI GPT + Whisper transcription API
google-genai>=1.0.0        # Gemini (NEW SDK — replaces deprecated google-generativeai)
ollama>=0.4.0              # Local LLM (Ollama must be running: ollama serve)

# ── THE VOICE (3-tier output) ──
elevenlabs>=1.0.0          # Tier 1: Tee's cloned voice (premium)
edge-tts>=6.1.0            # Tier 2: Free Microsoft neural voices
# pyttsx3                  # Tier 3: Offline fallback — optional, uncomment if needed

# ── THE EAR (perception layer) ──
sounddevice>=0.4.6         # Microphone capture (works on Python 3.14, unlike PyAudio)
numpy>=1.26.0              # Audio processing for sounddevice
# NOTE: Whisper transcription uses the 'openai' package above

# ── THE MEMORY (2-tier storage) ──
httpx>=0.27.0              # Supabase REST API client (cold memory)
# supabase                 # Full SDK — needs C++ Build Tools, httpx works fine instead

# ── CORE UTILITIES ──
python-dotenv>=1.0.0       # Load .env API keys
pyyaml>=6.0                # Configuration files (settings.yaml)
pydantic>=2.0.0            # Data validation
aiohttp>=3.9.0             # Async HTTP client
requests>=2.31.0           # Sync HTTP client

# ── FUTURE / OPTIONAL ──
# rich>=13.0.0             # Pretty terminal formatting
# loguru>=0.7.0            # Advanced logging
# streamlit>=1.30.0        # Dashboard UI (Phase 8)
